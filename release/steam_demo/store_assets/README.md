# Steam 商店素材

`store_assets_manifest.json` 是规格和状态的唯一清单。截图由 `tools/capture_steam_demo_screenshots.cmd` 从真实 Godot 场景生成，不使用 UI 拼图代替玩法画面。

胶囊图当前只保留目标文件名，不放占位图，避免把临时素材误当成可提交资产。提交前必须逐项核对像素尺寸、产品名可读性、内容准确性和 Steam 的最新图形素材规则。
