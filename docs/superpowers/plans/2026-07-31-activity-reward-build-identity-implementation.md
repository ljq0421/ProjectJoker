# 阶段 B 实施计划

## 1. 活动核心

1. 为动态轮数、固定手牌与逐轮计划写失败测试。
2. 扩展 `RoomDefinition` 与 `EncounterRunSetup`。
3. 让遭遇会话使用实例轮数并保持庄家第三轮限制逻辑不变。
4. 编译三个正式非标准房间。
5. 更新路线、摘要与未来情报文案。

## 2. 庄家奖励

1. 为候选生成、互斥选择、12 张替换和检查点恢复写失败测试。
2. 扩展区域奖励池与 `AreaRunSession` 状态。
3. 将现有刻印面板改为场景文件定义的混合奖励面板。
4. 完成稀有牌替换与刻印安装两条鼠标路径。

## 3. 构筑身份

1. 为三种身份分类和正式内容覆盖写失败测试。
2. 增加只读身份目录/分析器。
3. 路线、商店和奖励显示身份标签与套牌计数。

## 4. 验证与提交

1. 全量测试。
2. 金线、镜厅、无面输入/布局/端到端检查。
3. 非标准节点与混合奖励固定种子检查。
4. `git diff --check` 和差异审计。
5. 仅提交阶段 B 代码/数据；阶段 A、B 文档保持已暂存未提交。
