# 正式远征十八类规则池实施计划

> 对应设计：`2026-07-31-formal-rule-pool-design.md`
> 实施方式：严格 TDD，完成当前任务并验证后才进入下一任务
> 文档边界：只暂存，不进入阶段代码提交

## Task 1：建立失败的正式内容覆盖测试

- 新增 `tests/formal_rule_pool_test.gd`。
- 收集十二个普通房、两个固定庄家遭遇与无面主人三轮日程。
- 断言正式内容引用十八类模板中的全部十八类。
- 断言每区至少六类模板。
- 断言每组路线二选一模板签名不同。
- 先运行并确认当前只覆盖三类而失败。

## Task 2：建立普通房可达性门

- 扩展正式规则池测试，使用多个公开骰面样本枚举分配。
- 每个普通房至少找到两种让两张或以上规则台得分的分配。
- 桥接、回声和反向事件使用正式解析器，不写测试专用简化逻辑。
- 先确认新编排前或不合法参数会失败。

## Task 3：重编金线回廊

- 修改四个金线回廊房间资源和铁算盘遭遇。
- 更新名称、公开说明、参数、系数与必要目标。
- 更新 `gold_corridor_catalog_test.gd` 的模板签名。
- 运行目录、区域、路线情报和输入回归。

## Task 4：重编反照牌厅

- 修改四个反照牌厅房间资源和镜面夫人遭遇。
- 桥接台固定中轨，位置规则显示真实槽位。
- 更新 `mirror_hall_content_catalog_test.gd`。
- 验证镜像副本、左右方向、回声、桥接与预演一致。

## Task 5：重编无面中枢

- 修改四个无面中枢房间和三轮庄家日程。
- 保留独手裁定与三席到场的原有业务语义。
- 更新 `faceless_hub_content_catalog_test.gd`。
- 验证每轮公开日程、限制选择和新规则组合。

## Task 6：替换旧迁移契约

- 将 `rule_table_content_migration_test.gd` 从“保持三类旧语义”改为“正式内容引用目录且覆盖十八类”。
- 保留 51 个正式规则实例数量契约。
- 不修改规则档案室内容和教学夹具。

## Task 7：平衡与完整验证

- 运行新增测试和相关内容测试。
- 运行三区固定种子端到端、商店情报、引导、布局和真实输入。
- 运行全量测试和主场景 headless。
- 渲染路线、遭遇、庄家和完成页的 1280×720 / 1920×1080 证据。
- 检查日志错误标记和 `git diff --check`。

## Task 8：提交边界

- 设计与实施文档只暂存，不提交。
- 使用显式代码/数据路径或 `git commit --only` 提交阶段 A。
- 排除当前音乐、设置、主菜单和其他既有工作区改动。
